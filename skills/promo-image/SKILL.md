---
name: promo-image
description: 法拍房宣传海报合成——将房源图+话术拼接为「顶部标题+中部原图+底部话术」海报图片，并写回数据库 data 字段。何时触发: 需要生成法拍房宣传海报、图片拼接话术、图片加文案时,触发关键词包括"宣传海报"、"话术图"、"图片拼接"、"合成图"、"海报生成"。
---

# promo-image 法拍房宣传海报合成

逐张房源图合成宣传海报,并写库。

## 触发场景

- 用户要求给某套房源生成带话术的宣传图
- 需要批量为多套房源合成海报
- 下游视频脚本技能需要已填充话术的素材

## 使用方式

### 人工直接运行(CLI)

```bash
# 单套
python skills/promo-image/scripts/compose.py --source gpai --item-id 52885

# 批量(前5套)
python skills/promo-image/scripts/compose.py --all --limit 5

# 指定输出宽度(默认自动取该房源图片最大宽度)
python skills/promo-image/scripts/compose.py --source gpai --item-id 52885 --width 1080
```

### Agent 调用

```python
import sys; sys.path.insert(0, ".")
from skills.promo_image.scripts.compose import run, run_all

run("gpai", "52885")           # 单套
run_all(source="gpai", limit=5) # 批量
```

## 数据契约

- 输入:房源在 DB 中已采图(`data.images` 非空)
- 输出:
  - 磁盘: `assets/{source}/{item_id}/imgs/poster_{nn}_v.png`(竖版 9:16)、`poster_{nn}_h.png`(横版 16:9)
  - DB: `data.script` = 完整口播稿; `data.script_images` = `[{file, copy, angle, source_img, orientation}]`
- 话术模板来源: `assets/短视频宣传话术.csv`

## 合成逻辑

- **双版式**:每套房源每张原图各生成一张竖版(9:16)和一张横版(16:9),适配不同平台
- **统一尺寸**:每套房源所有海报共用同一 canvas,**不硬编码**——竖版宽=原图最大宽、高=宽×16/9;横版高=原图最大高、宽=高×16/9;再叠加上下标题/话术带(由字体度量推导)
- **标题带**:顶部,**水平+垂直居中**,过长则按字符宽度自动**换行**(竖版窄更易爆行,字号自适应缩小保证图片区下限);字体 `assets/fonts/江城律动圆.ttf`
- **图片带**:中部,每张原图按 contain 缩放,居中放置
- **话术带**:底部,**水平居中**,按字符宽度自动换行;字体 `assets/fonts/极影毁片圆.ttf`
- **标点清理**:填充后文本经 `_clean_text` 处理——合并连续标点(`,，`→`,`、`：；`→`：`)、去行首空占位符产生的标点,**保留句末正常！？。**(不误删)
- **图↔文案分配**:首图=开场钩子(吸睛),末图=行动号召(收尾),中间按逻辑序(硬指标→价格→地段→常见误区→风险→紧迫感)循环分配给各张图;图数>角度数则回卷,不截断

## 字段提取兼容键

| 字段 | 候选 property_info 键 |
|------|----------------------|
| 小区名称 | 小区名称/标的物名称/拍品名称/项目名称,缺则 title |
| 区域 | title/坐落 中首个「区/县/市」前片段 |
| 建筑面积 | 建筑面积/建筑总面积/房屋建筑面积/套内面积 |
| 户型 | 户型/房屋户型/户型、朝向等情况 |
| 朝向 | 朝向/房屋朝向 |
| 所在楼层 | 所在楼层/房屋楼层 |
| 总层数 | 总层数/总楼层 |
| 装修 | 装修情况/装修程度 |
| 房龄 | 房产年龄 |
| 权利限制 | 权利限制情况/抵押情况/查封情况 |
| 税费 | 税费负担/税、费承担/税费情况 |
| 腾空交付 | 是否已腾空/腾空情况/占用情况 |
| 周边配套 | 周边配套 |
| 坐落 | 坐落 |
| 保证金 | 保证金/保证金和增价幅度 |
| 增价幅度 | 增价幅度/加价幅度 |

## 注意事项

- 占位符填充时:若该行依赖的字段缺失(如 gpai 无 POI),则跳过该行尝试下一行;整角度无可填充行则跳过该角度
- 派生占位符: `{折扣率}` = 起拍/参考×10; `{单价}` = 起拍元/建筑面积/1e4; `{省额}` = 参考-起拍; POI 距离 = 各分类 min(distance)
- `poster_*.png` 写入 `imgs/` 目录,沿用 `data.images[].file` 约定(仅存文件名)
